#pragma once

namespace hyperlog::constants{

  // header constants
  constexpr auto RID_HEADER = "X-RID";
  constexpr auto STREAM_HEADER = "X-Stream";
  constexpr auto DEADLINE_HEADER = "X-Deadline-Us";

  // storage constants
  constexpr auto DEFAULT_STREAM = "default";
}